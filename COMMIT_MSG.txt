feat: card validation, simulated banks, profile photo, QR payment redirect, total_paid fix

Card payments:
- Add Luhn algorithm, card brand detection, format validation
- Add bank/payment method selector (12 Venezuelan banks + Zinli/PayPal/Stripe)
- Real-time validation feedback with error/success states
- Send bankName in payment request

QR flow:
- Backend returns firstInstallmentId after transaction creation
- Redirect to /cuotas/{id} to pay first installment
- Success screen with "Pay first installment" button

Profile:
- Add profile photo upload (base64, PUT /users/profile-photo)
- Show photo in sidebar, topbar, and profile page
- Hide user ID from profile (keep private)
- Migration V20: add profile_photo_url column

Total paid fix:
- IncrementUserTotalPaid now called in payment transaction
- total_paid and installments_paid_count properly increment

Tours:
- skipTour now marks tour as seen (same as endTour)
