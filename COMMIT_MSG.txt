feat: PWA support for all 4 apps + mobile responsive improvements

PWA:
- Add manifest.ts, sw.js, ServiceWorkerRegister to web-admin, web-merchant, web-landing
- Add viewport export with themeColor to all 3 apps
- Add appleWebApp metadata to all 3 apps
- Add ServiceWorker i18n namespace to all 3 apps

Responsive:
- Add mobile bottom nav to web-patient (Dashboard, Pagar, Cuotas)
- Fix form grids: grid-cols-2 -> grid-cols-1 sm:grid-cols-2 in all modal forms
- Fix modal widths: add w-full mx-2 for mobile fit
- Fix search inputs: w-full sm:w-64 instead of hidden sm:block w-64
- Show hero card on mobile in web-landing
- Improve touch targets in web-landing navbar
- Add safe-area-inset padding to modals
- Improve cart quantity buttons touch targets in web-patient
